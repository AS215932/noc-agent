# NOC shadow learning-event fixtures

These fixtures mirror the `learning_ledger_v1` contract from
`AS215932/knowledge`. They are sanitized shape tests only and must not contain
live MCP/Prometheus/Icinga payloads, raw logs, commands, packet data, or secrets.
